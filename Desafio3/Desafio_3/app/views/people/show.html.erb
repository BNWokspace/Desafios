<p id="notice"><%= notice %></p>

<section class= "standard-content">
  <div class="card" style="width: 30rem;">
     <div class="card-body">
        <h5 class="card-title">Dados da Compra</h5>
        <p class="card-text">
        <strong>ID:</strong>
        <%= @person.id %>
        </br>
        <strong>Nome:</strong>
        <%= @person.name %>
        </br>
        <%= link_to 'Edit', edit_person_path(@person) , class: 'btn btn-warning' %>
     </p>
    </div>
  </div>

<%= link_to 'Back', people_path , class: 'btn btn-outline-primary rails-btn back-edit' %>
</section>